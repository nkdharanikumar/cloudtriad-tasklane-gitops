apiVersion: v1
kind: Service
metadata:
  name: tasklane-frontend
  namespace: tasklane
spec:
  type: ClusterIP
  selector:
    app: tasklane-frontend
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
